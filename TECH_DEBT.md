# Comprezza — Technical Debt

This file records intentional compromises. Never hide a shortcut; add it here with reason, impact, future fix, and priority.

| TD-043 | Phase 15 release documentation requires owner/legal replacement values | Privacy, terms, copyright, security, support, pricing, and public URLs cannot be safely invented by engineering | Replace bracketed values, publish reviewed HTTPS policy/legal pages, and archive approval before Play submission | Critical | Open |
| TD-044 | Phase 15 store/storyboard assets are specifications, not binary artwork | Play upload cannot proceed until exact-AAB screenshots and graphics are produced and reviewed | Produce, capture, license, checksum, and archive final assets | High | Open |
| TD-045 | Phase 15 Data Safety answers require exact-AAB/transitive dependency confirmation | Static source intent alone cannot prove Play declarations | Run dependency/license/security audit and submit Data Safety for the exact artifact | Critical | Open |
| TD-046 | Batch picker seam (`BatchImagePicker`) has no error channel | A failed pick (e.g. permission denial) is indistinguishable from cancel and surfaces as an empty queue | Add an error channel or fallback notice when the picker returns a failure | Low | Open |

| TD-040 | Final engineering audit withholds all release-track approval at 76/100 | Draft privacy policy, placeholder/static commercial surfaces, transitional Settings execution, reusable localization gaps, and missing CI/device evidence remain open | Close implementation/product gates, execute protected CI/device matrix, publish legal policy, and obtain release-owner approval | Critical | Open |
| TD-041 | Compressor disposal can cancel export/share staging but not native codec work | Frozen compression gateway has no cancellation contract; disposed routes ignore late results while native work may continue | Add a reviewed cancellable native processing contract during the approved migration; do not expand scope in the frozen architecture | High | Open |
| TD-042 | Final audit found hardcoded English strings in reusable, mostly unreachable design-system components | A future caller could ship non-localized UI and fail localization review | Route component labels/defaults through generated localization at the next approved presentation cleanup | Medium | Open |

| TD-037 | Phase 14 production audit cannot execute Flutter/Android/CI/device validation in the current environment | Analyzer, tests, AAB/R8, merged manifest, accessibility, low-memory, OEM, battery, and Play evidence remain unproven | Run the protected RC1 pipeline and device matrix before production approval | Critical | Open |
| TD-038 | Public privacy policy remains a legal/product input rather than an engineering-generated artifact | Policy text is finalized; Play submission still requires the five personal fields, legal review, and a published HTTPS URL | Fill the personal fields, publish a legally reviewed HTTPS policy, and complete Data Safety | Critical | Open |
| TD-039 | Workflow screen-reader progress context required a localized positional message | Hardcoded English semantics reduced localization quality and RTL readiness | Resolved by `workflowStepPosition(current, total, label)` in both workflows | Medium | Resolved |

| ID | Reason | Impact | Future Fix | Priority | Status |
|---|---|---|---|---|---|
| TD-001 | Preserve the existing prototype while architecture was hardened | Legacy controller/services are still outside the final Provider/use-case path | Migrate the compressor feature and remove `LegacyCompressorAdapter` | High | Open |
| TD-002 | Defer package/toolchain validation because Flutter SDK was previously unavailable | Flutter/Dart generation, formatting, analyzer, and unit tests are now verified; Android packaging remains separate | Keep the passing Flutter validation matrix in CI and retain Android release validation as a gate | High | Partially resolved |
| TD-003 | Keep the initial theme catalog minimal before the full design system is migrated | Existing prototype widgets still contain repeated visual literals | Migrate all UI to `AppDimensions`, `AppRadius`, `AppElevations`, typography, and motion tokens | Medium | Open |
| TD-004 | Use manual DI before introducing `injectable` | Dependency graph remains hand-maintained as feature count grows | Re-evaluate after multiple feature graphs exist; preserve explicit ownership if adopting code generation | Low | Open |
| TD-005 | Flutter/Dart SDK was unavailable during the initial architecture review | Flutter/Dart generation, formatting, analyzer, and unit tests are now verified locally; Android build evidence is still missing | Run `flutter build apk --release` in Java/Android-SDK-enabled CI before release sign-off | High | Partially resolved |
| TD-006 | Avoid an unreviewed dynamic-color package/native adapter in the foundation | Wallpaper-derived Material You colors are deferred behind a replaceable source | Select and review a maintained adapter when product requirements justify it | Medium | Open |
| TD-007 | Device SDK/storage values use safe nullable fallbacks without platform calls | Consumers cannot yet display those optional capabilities | Add a permission-free Android adapter and device tests when needed | Medium | Open |
| TD-008 | Cache cleanup is policy-limited but not yet chunked/cancellable | Extremely large caches may consume excess post-frame CPU/battery | Add bounded batches, cancellation, and scheduling policy before large-cache release | Medium | Open |
| TD-009 | The transitional legacy controller still owns feature orchestration while the final Provider/use-case migration is pending | Controller/gateway and widget coverage is narrower than the final production target | Complete the Phase 3 migration and add controller, lifecycle, and widget tests | High | Open |
| TD-010 | `image_preview_card.dart` constructs a dart:io File at the final UI rendering edge | Current Android UI is fine, but cross-platform rendering remains coupled to local files | Introduce a platform-neutral image-source presentation adapter if additional platforms are enabled | Low | Open |
| TD-013 | Phase 3 baseline analyzer uses filename/size heuristics instead of image-content or ML analysis | Recommendations are deterministic but confidence is limited | Add a reviewed on-device feature extractor/ML adapter behind `ImageAnalyzerEngine` | Medium | Open |
| TD-014 | Metadata policy is applied through codec keep/remove options; standalone EXIF rewriting is not implemented | Dedicated metadata operations cannot yet rewrite EXIF independently | Add and test a local EXIF adapter before exposing standalone metadata workflows | Medium | Open |
| TD-015 | Legacy compression service remains alongside the Phase 3 native engine | Codec behavior can drift until the UI migration is complete | Migrate the legacy adapter to `EngineManagerDataSource` and remove duplicate codec paths | High | Open |
| TD-016 | Data-layer adapters construct `File` handles for output stat/cleanup while using the filesystem service for ownership operations | A stricter path-only boundary is not yet enforced at every adapter edge | Extend `FileSystemService` with path-based metadata/deletion methods and remove direct handle construction | Medium | Open |
| TD-017 | Native codec and Android lifecycle behavior is not device-tested in this environment | Codec support, EXIF behavior, and low-memory behavior remain platform-dependent | Add Android integration and low-memory device tests in CI | High | Open |
| TD-018 | Phase 4 folder scanning uses a path-based foundation before Android SAF/DocumentFile integration | Real user-selected folder traversal and persisted URI permissions are not yet device-backed | Add a reviewed SAF adapter and Android integration tests before folder compression release | High | Open |
| TD-019 | File validation performs stat, streaming checksum, and native decode as separate safety gates | Large files may be read more than once before processing | Add a coordinated metadata/checksum/decode pipeline only after profiling real devices | Medium | Open |
| TD-020 | Preserve compatibility while hardening only the generated-write path | Lower-level `writeBytes`, `copy`, and `move` are not yet uniformly atomic/serialized | Route all generated writes through one atomic, locked filesystem primitive before exposing additional writers | High | Open |
| TD-021 | Keep external-picker adapters independently testable while the filesystem contract is stable | Some Phase 4 services still use direct metadata checks for external or managed paths | Add path-based metadata/existence operations to `FileSystemService` and remove direct file probes from services | Medium | Open |
| TD-022 | Android integration requires an Android-enabled environment | SAF/MediaStore/Photo Picker lifecycle, OEM codec, and low-memory behavior are not device-verified | Run Android integration, low-memory, and release-build validation before production approval | High | Open |
| TD-023 | Settings personalization was initially isolated before the minimum root integration was approved | Dynamic colors, color-blind palette transformation, and platform screen-reader adapters are not yet specialized; device accessibility validation remains | Add reviewed platform adapters only if required and run TalkBack/large-text/reduced-motion tests | Medium | Partially resolved |
| TD-024 | Settings file picker/share and policy/rating destinations require approved product/platform contracts | Clipboard import, system share, and the Play-store/website destinations (url_launcher) are wired; the real website domain and document-picker import remain product inputs | Supply the approved website domain and document-picker integration; verify Play/privacy behavior | High | Partially resolved |
| TD-025 | Frozen legacy workflow owns execution defaults independently from Phase 10 preferences | Quality, format, resize, target-size, and metadata (keepExif) are now workflow execution inputs; persisted Settings preferences are still not wired into the workflow | Wire the complete Settings snapshot through the approved post-freeze workflow contract | High | Partially resolved |
| TD-026 | Settings exposes cache/history retention preferences before every cleanup policy is connected to the Settings mutation path | Users can persist retention values while enforcement remains owned by existing cleanup services | Connect retention preferences to the reviewed cleanup scheduler/policy contract and add device-scale tests | Medium | Open |
| TD-027 | Phase 11.1 uses a narrow compatibility adapter because the frozen controller gateway exposes only a file path | Export reports in the compatibility workflow contain minimal derived metadata until the final workflow contract is migrated; the adapter now explicitly supplies app-managed destination and naming policy | Migrate the compressor workflow to the approved rich export request contract without changing security boundaries | Medium | Open |
| TD-028 | Temporary share files must outlive asynchronous Sharesheet recipient reads | Immediate deletion can break recipient apps; cleanup is delegated to TTL policy | Add lifecycle-aware managed cleanup scheduling and Android recipient-device coverage | High | Open |
| TD-029 | Android/device integration for the Phase 11.1 coordinator is unavailable in the repository environment | Managed coordinator is now registered and reachable through the production gateway adapter, but Sharesheet/MediaStore/OEM behavior is unverified | Run Android profile/release, recipient lifecycle, OEM, and failure recovery tests | High | Partially resolved |
| TD-030 | Phase 11.1 intentionally retains successful temporary share files through the existing startup TTL policy | This avoids deleting files while recipient apps may still read them; process-death/OEM timing remains unverified | Add lifecycle-aware cleanup scheduling only if Android recipient testing demonstrates startup TTL is insufficient | Medium | Partially resolved |
| TD-031 | Flutter/Android profiling environment is unavailable in the repository shell | Cold/warm startup, native heap, frame timing, thermal, and low-end-device targets cannot be proven locally | Run profile/release traces and low-memory/thermal matrices in Android-enabled CI | Critical | Open |
| TD-032 | Cache cleanup scans generated directories sequentially after the first frame | Very large caches can still consume post-frame CPU and I/O | Add bounded/cancellable cleanup batches after profiling real cache sizes | Medium | Open |
| TD-036 | Launcher assets were previously represented by an Android system placeholder | Store presentation and adaptive/monochrome launcher quality were not release-ready | Validate branded adaptive icon, legacy fallback, monochrome treatment, and splash rendering in the signed artifact | High | Partially resolved |
| TD-033 | Cross-phase validation cannot execute Flutter or Android tooling in the repository shell | Analyzer, tests, format, manifest merge, device lifecycle, accessibility, and profile/release evidence remain unavailable | Run the combined validation matrix in Flutter/Android-enabled CI before production sign-off | Critical | Open |
| TD-034 | Phase 13 release preparation cannot verify resolved Flutter target SDK, AAB output, R8, signing, or merged manifest without the Android toolchain | Play target compliance, shrinker behavior, upload signing, and artifact integrity remain unproven; fail-fast signing and CI workflow are now configured | Run RC1 release pipeline with current target API, protected upload key, AAB inspection, and Play Console validation | Critical | Open |
| TD-035 | CI workflow is newly checked in but has not executed in the repository's GitHub environment | Automated analyze/test/AAB/lint/artifact checks, lockfile enforcement, ephemeral Gradle-wrapper generation, R8 mapping retention, and secret cleanup are configured but unproven | Execute and review protected CI runs before production release; do not store signing secrets in the repository | High | Open |
